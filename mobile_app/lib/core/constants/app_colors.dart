import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Synced with web variables.css)
  static const Color primary = Color(0xFF4F46E5);      // Indigo Royal Blue
  static const Color primaryLight = Color(0xFFEEF2FF);
  static const Color primaryDark = Color(0xFF3730A3);

  static const Color accentGreen = Color(0xFF059669);   // Emerald Green (Urgent badge / highlights)
  static const Color accentGreenLight = Color(0xFFECFDF5);

  // Surface & Background Colors
  static const Color bgLight = Color(0xFFF8FAFC);
  static const Color cardLight = Color(0xFFFFFFFF);
  static const Color borderHairline = Color(0x140F172A); // rgba(15, 23, 42, 0.08)

  static const Color bgDark = Color(0xFF0F172A);
  static const Color cardDark = Color(0xFF1E293B);

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textLight = Color(0xFFF8FAFC);
}
